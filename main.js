//TYPESCRIPT TUTORIAL
//VARIABLES: 
var hello = "world";
//FUNCTIONS:
//You must specify the type of the arguments 
var getFullName = function (name, surname) {
    return name + " " + surname;
};
console.log(getFullName("Monster", "Lessons"));
//OBJECTS:
//Object using the interface User for properties
var user = {
    name: 'Monster',
    age: 3
};
var user2 = {
    name: "Jack"
};
console.log(user.name);
